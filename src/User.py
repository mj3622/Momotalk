class User:
    def __init__(self, id, name, sex, birthday, mail, info):
        self.id = str(id)
        self.name = str(name)
        self.sex = str(sex)
        self.birthday = str(birthday)
        self.mail = str(mail)
        self.info = str(info)



